# 민낯(술자리) — 현재 상태

> 최종 갱신: 2026-09-18
> 이 문서가 프로젝트 현황의 기준점이다. `plan.md` · `plan-v1.1.md` · `SAJU_BUILD_PLAN.md`
> 는 실제 코드보다 뒤처져 있으니 그대로 믿지 말 것 (아래 "계획 문서 실태" 참조).

---

## 한 줄 요약

3개월간 멈춰 있던 파이프라인을 되살렸다. **원인은 하나가 아니라 넷**이었고
(크레딧 소진 · 수집원 5개 사망 · 국회 API 5건 제한 · Supabase 프로젝트 삭제),
전부 **조용한 실패**라 아무도 몰랐다. 지금은 DB·수집원·비용 구조가 복구됐고,
남은 것은 크롤러 정기 실행 재개다.

---

## 무엇이 고장나 있었나 (2026-09-16 진단)

| # | 문제 | 어떻게 숨어 있었나 |
|---|---|---|
| 1 | Anthropic 크레딧 소진 | 7월 하순 실행분 전체가 `credit balance is too low` 400. 예외를 `try/except` 가 삼켜 **워크플로가 초록색 success** 로 끝남 |
| 2 | 수집원 8개 중 5개 사망 | RSS 연합(404)·KBS(파싱실패)·MBC(301), 팩트체크 3사 0건, 법원 302. 실제 가동은 **SBS 1곳 + 네이버** 뿐 |
| 3 | 국회 API 5건 제한 | 인증키 없이도 `200 OK` 에 `list_total_count: 299` 를 주면서 **실제 행은 5개**. sample key 는 `pSize` 5 고정 |
| 4 | Supabase 프로젝트 삭제 | 크롤러가 멈추자 DB 무활동 → 무료 플랜 정지 → 삭제. 슬롯(계정당 2개)도 꽉 차 있었음 |
| 5 | GitHub Actions 자동 정지 | 저장소 60일 무활동으로 `disabled_inactivity`. 마지막 실행 7/25 |

3번이 특히 아팠다. 정치인 DB 가 54명에 묶였는데, 프롬프트가 진영(camp)을
"오직 DB 명단으로만 결정"하도록 못박아 둔 탓에 **나머지 의원이 등장하는 기사는
전부 폐기**됐다. 돈은 다 쓰고 결과는 버리는 구조였다.

---

## 무엇을 했나

### A. 수집원 복구

| 항목 | 전 | 후 |
|---|---|---|
| RSS 매체 | SBS 1곳, 26건 | **10곳, 383건** (진보3·중도4·보수3) |
| 정치인 DB | 54명 | **319명** (blue 202 / red 117) |
| 국회 법안 | 5건 | **100건** |
| 팩트체크 | 4곳 중 3곳 사망 | 비활성화 (아래 참조) |
| 법원 | 302 에러 | URL 수정, 파싱 정상 (정치인 사건은 거의 없음) |
| 디시 트렌드 | 0건 지속 | 제거 |

- 매체명은 `config.py` 의 `MEDIA_LEAN` 키와 정확히 일치시켰다 (편향 감사 전제)
- **매체별 라운드로빈 할당** 도입. 앞에서 N개를 자르면 기사 많은 연합뉴스(86건)가
  슬롯을 독식해 다양성이 무너진다. 현재 매체당 6건씩, center 24 / progressive 18 /
  conservative 18 로 배분
- 국회 API 는 `nwvrqwxyaytdsfvhu` → **`ALLNAMEMBER`** 전환. 역대 3,296명 중
  `GTELT_ERACO` 로 22대만 필터

### B. 설계 변경 — 진영 판정은 추론이 아니라 조회

명단을 역대 3,296명까지 넓히면 프롬프트가 10만 자가 되어 월 $190이 된다.
품질과 비용이 정면충돌하는 지점에서 **프롬프트에서 명단을 빼고 코드가 DB를 조회**
하도록 뒤집었다 (`analyzer.resolve_camp`).

- 시스템 프롬프트 **7,787자 → 2,915자**
- 명단이 319명으로 늘어도 **호출 비용 불변**
- LLM 환각으로 인한 진영 오분류가 구조적으로 제거됨
- 안전성 검증: 3,296명 전수 분석 결과 **22대 범위에서 이름→진영 충돌 0건**
  (박지원 2명은 둘 다 민주당, 조정훈 충돌 상대는 1914년생 제2대 의원)
- `reasoning` 을 결론 *앞* 으로 옮겨 사후정당화 → CoT 로 전환 (비용 동일)

### C. 비용 구조

- **LLM 호출 *전* URL 중복 제거** (`drop_seen`). 기존 중복 판정은 AI 가 생성한
  headline 이 키여서, 이미 본 기사도 분석비를 다 낸 뒤 버려졌다
- 소스별 예산 상수화 — RSS 60 / 네이버 30 / 법안 20
- 사주 모델 ID `claude-sonnet-4-6` → `claude-sonnet-5` ($3/$15 → $2/$10)
- 크롤러 매칭 모델 `claude-sonnet-4-20250514` → `claude-sonnet-5`

### D. 관측 가능성 — "조용한 성공"을 없앴다

이번 사태의 근본 원인이다. 이제:

- 토큰 사용량·추정 비용을 매 실행 출력 (`[비용] 호출 N회 … 추정 $X`)
- 프롬프트 캐시 미적중 시 경고
- 버려진 이유를 집계해 출력 (진영 판정 불가 / JSON 파싱 실패 / API 오류 …)
- 수집 0건 소스를 경고로 표시
- **전량 실패 시 `exit 1`** — 대시보드가 빨갛게 뜬다

### E. DB 복구 경로 정비

`supabase/` 는 17개 마이그레이션을 SQL Editor 에 손으로 순서 맞춰 붙여넣어야
복구되는 구조였고, **파일명 순서와 실제 의존성 순서가 두 군데 달랐다**:

- `migration-v1.1.sql` 이 `issue_clusters` 를 CREATE 하고 `migration-003` 이 ALTER
  → **v1.1 이 003 보다 먼저**
- `issues.category` CHECK 제약이 4개 파일에서 재정의됨 (`schema`→`002`→`v1.1`→`008`)
  → **008 이 v1.1 뒤**여야 크롤러 `config.py` 의 `ALL_CATEGORIES` 와 일치

파일명대로 돌리면 전자는 에러, 후자는 **에러 없이 조용히 잘못된 카테고리 집합**이 된다.

- `supabase/SETUP.sql` — 의존성 순서 재배열 + 멱등 변환한 단일 실행 스크립트
- `supabase/SEED.sql` — 시드 6개 통합 (선택)
- `supabase/README.md` — 복구 런북, 순서 근거, 로컬 Postgres 검증 절차
- 파괴적 구문 가드 2개 — `TRUNCATE issues`(v1.1 원본 동작)와
  `DROP TABLE board_posts`(007). 데이터가 있으면 아무것도 실행하지 않고 중단

### F. 테스트

`minnat-crawler/tests/` 신설 13케이스 + CI 게이트. 실제로 한 번 깨졌던 성질을 못박았다:

- 매체별 균등 할당 (한 매체 독식 방지)
- **중복 제거가 예산 배분보다 먼저** — 순서를 반대로 짰다가 기처리 기사가 슬롯을
  차지해 신규 기사가 영영 분석되지 않는 버그를 만들었다
- `drop_seen` 이 `seen_urls` 를 변경한다는 성질 (같은 리스트에 두 번 쓰면 전부 사라짐)
- camp 판정이 DB 조회이고 프롬프트에 명단이 없다는 것

---

## 검증 결과

### DB (Postgres 17.6, 격리된 로컬 DB → 이후 신규 Supabase 프로젝트)

| # | 시나리오 | 결과 |
|---|---|---|
| 1 | 빈 DB → `SETUP.sql` | 에러 0. 테이블 19 / RLS 정책 46 / 인덱스 54 |
| 2 | 빈 DB → 재실행 | 에러 0 (멱등) |
| 3 | `SEED.sql` | 에러 0. 정치인 102 / 이슈 66 / 대통령 14 |
| 4 | 데이터 有 → `SETUP.sql` 재실행 | 사전 점검이 중단, 이슈 66건 보존 |

`issues_category_check` 17종이 크롤러 `config.py` 의 `ALL_CATEGORIES` 와 완전 일치.

### 신규 Supabase 프로젝트 (2026-09-16 적용 완료)

| 검증 | 결과 |
|---|---|
| service_role 접근 (크롤러) | 15개 테이블 전부 ✅ |
| anon 읽기 (프론트) | 8개 테이블 HTTP 200 ✅ |
| **anon 쓰기 시도** | **HTTP 401 차단** ✅ |
| 페이지 | `/ /issues /politicians /politicians/presidents /explore /about /board /saju` 전부 200 ✅ |
| 적재 데이터 | 이슈 66 · 정치인 102 · 대통령 14 · 공약 44 · 클러스터 51 |

프로젝트 생성 시 보안 설정은 **Data API ON / 새 테이블 자동 노출 ON / 자동 RLS ON**.
`SETUP.sql` 에 `GRANT` 문이 없어서 "자동 노출"을 끄면 프론트가 전부
`permission denied` 가 난다. 자동 RLS 가 켜져 있어 보안상으로도 안전하다.

---

## 알려진 한계 (고칠 수 없어서 정직하게 남긴 것)

### 팩트체크 — 국내 소스 전멸

`factcheck_false` 는 "IFCN 인증 매체의 false 판정"만 인정하는데, 조건을 만족하는
국내 매체가 남아 있지 않다:

| 매체 | 상태 |
|---|---|
| SNU팩트체크 | 2024-08 무기한 중단, 현재 SSL 인증서도 만료 |
| 팩트체크넷 | 종료 |
| JTBC 팩트체크 | 섹션 URL 404 (유일한 국내 IFCN 인증이었음) |
| 뉴스톱 | 도메인이 타 매체(newstopkorea.com)로 이전 |
| 연합뉴스 팩트체크 RSS | 404 |
| MBC 알고보니 / KBS 팩트체크K | 0건 |

SBS '사실은'은 IFCN 인증이 아니고, 어차피 SBS RSS 로 함께 들어온다.
**비인증 매체를 억지로 넣어 방법론을 위반하는 대신 수집을 중단**하고 사유를
`crawlers/factcheck.py` 주석에 남겼다. 새 인증 매체가 생기면 `ENABLED` 만 켜면 된다.

→ **미결정**: `/about` 방법론 페이지에 이 사유를 공개할지 (plan-v1.1 "한계 솔직 공개" 원칙)

### 법원 — 판결문 익명화

법원은 판결문에서 피고인을 익명화한다(「피고인」「甲」). 법제처 판례 API 도
마찬가지여서 **정치인 개별 매칭이 구조적으로 불가능**하다. 정치인 형사·민사 판결의
실용적 1차 소스는 뉴스 보도다. 법원 크롤러는 URL 을 고쳐 보조 소스로 두되
0건이 정상일 수 있다.

### 진영 미매핑 2명

22대 321명 중 **윤종오(진보당)** 와 **한동훈(무소속)** 이 `PARTY_CAMP` 에 없다.
무소속은 의도적 제외가 맞다(프롬프트 원칙). 진보당은 정치적 판단이 필요해
임의로 넣지 않았고, 실행 시 경고로 표시된다.

→ **미결정**: 진보당을 진영 매핑에 포함할지

### Supabase 무료 플랜

약 1주 무활동 시 정지, 오래 정지되면 삭제된다. 이전 프로젝트가 그렇게 사라졌다.
**크롤러가 정기적으로 돌면 그 자체가 DB 활동이라 정지되지 않는다 —
크롤러를 살려두는 것이 곧 DB 를 지키는 것이다.**

---

## 2026-09-17~18 — 사주 풀이 개편 · 기사 상세 재구성 · 임베딩 장애 규명

전부 `main` 머지 완료 (2026-09-18 기준 `minnat` #1~#8 · `minnat-crawler` #1~#3, 열린 PR 없음).
마이그레이션 019·021·022·023 프로덕션 적용 완료, Anthropic·OpenAI 크레딧 충전 완료.

### A. 규명한 근본 원인 — 임베딩이 통째로 죽어 있었다

`match_similar_events` 가 유사도로 문자열 `"NaN"` 을 돌려주는 걸 발견해 끝까지 팠다.

```
OpenAI 크레딧 소진(insufficient_quota, HTTP 429)
  → get_embedding() 이 실패 시 [0.0]*1536 을 반환
  → 영벡터가 issue_clusters.embedding 에 저장
  → pgvector 의 `<=>` 가 NaN (코사인 거리의 분모가 0)
  → Postgres 는 IEEE 와 달리 NaN 을 "모든 수보다 큰 값" 으로 취급
  → NaN >= 0.5 임계값 통과 + ORDER BY ... DESC 에서 1위
```

**프로덕션 실측: 클러스터 67건 중 NULL 51 / 영벡터 16 / 정상 0건.**

결과는 두 가지였다.
1. "유사 사례" 가 유사도와 무관한 무작위 목록이었다. 이전 UI 에서는 10px 회색이라
   `유사도 NaN%` 가 보이지도 않아 계속 깨진 채로 있었다.
2. **Stage 2 사건 매칭(임베딩 비교)도 몇 달간 사실상 꺼져 있었다.**
   `CLAUDE.md` "알려진 이슈 #1 — dedup 미흡" 의 원인 중 하나로 보인다.

Anthropic 크레딧 때와 같은 **조용한 실패**다. 파이프라인은 계속 초록색이었다.

### B. 사주 — 풀이가 단순하다는 지적 (PR #2)

경쟁 서비스(사주아이) 유료 리포트 3종과 결과 화면을 대조해 표로 만든 뒤 고쳤다.

| | 전 | 후 |
|---|---|---|
| 종합 섹션 | 6개 (연애·직업은 "딱 2문장") | 10개 |
| 본문 | "4~6문장" 자유 산문 | 3문단 고정 (현상 → 원국 근거 → 이번 주 행동), 240~320자 |
| 제목 | 기능 라벨 | `label`(고정 카테고리) + `title`(원국에서 나온 후킹 한 줄) |
| 개운법 | 없음 | 처방 섹션 끝에 `색 · 숫자 · 방향 · 오늘 할 것` (용신 기준) |
| 궁합 | 4섹션 | 6섹션, 첫 title 이 점수 아래 캐치프레이즈 |

화면은 섹션 카드(번호·라벨·훅 제목·문단 분리), 목차 칩, 입력한 고민 되돌려주기,
로딩에 "N편을 쓰는 중 + 나올 목록".

**같이 고친 버그** — 본문이 여러 문단이 되자 모델이 JSON 문자열 안에 날 줄바꿈을 넣어
`JSON.parse` 가 죽었다(= 전 요청 502). 타입체크·빌드는 전부 통과한 상태였고
**실제 LLM 호출 1번을 해봐야** 드러났다. `lib/saju/json-repair.ts` 로 복구 + 테스트 8건.

**비용** — full 출력 3,000 → 약 5,100토큰. 회당 약 $0.033 → **$0.053**.
**2026-09-18 에 기본값을 3000 → 800 으로 잡았다 — 기준은 목표 DAU 20.**
크레딧이 하루 1개라 DAU 20 이면 하루 최대 20 호출, 30일이면 **600 이 이론 천장**이다.
7일 연속 보너스(20명 × 30/7 ≈ 86)와 공유·초대분을 얹으면 700~750, 킬스위치가 정상 사용을
끊으면 안 되니 10% 여유를 더해 **800 (월 최대 약 $42)**.
상한이 세는 건 사람 수가 아니라 **캐시 미스 LLM 호출 수**다 — 같은 차트·타입·고민을 다시
열어보는 건 0회이고, 타입 변경·고민 변경·오늘의 사주 다음 날·새 궁합 상대만 1회씩 는다.
그래서 실제 소진은 800 보다 적게 나갈 가능성이 높다.

### C. 사주 — 무료 정책 v4 "크레딧 하나로" (PR #7 머지, 023 적용 완료)

v3 는 `dailyFree`(불리언) + `balance`(획득분) 두 개념이 따로 돌아 배지 문구가 네 갈래였다.
이제 통화는 크레딧 하나다. 풀이 한 편 = 크레딧 1, 자정(KST)에 **1까지 채운다(top-up)**.
화면은 `N/1` 로만 보여준다.

쌓지 않는 이유: 매일 +1 로 누적되면 안 보고 묵혀 30개를 만들 수 있고 킬스위치 비용 모델이 깨진다.
배지 아이콘은 6px 점 → 15px 코인(찬 금색 / 빈 테두리)으로 바꿔 있음·없음이 한눈에 갈린다.
자정 충전은 아이콘 옆 툴팁으로 설명한다.

### D. 정치 — 기사 상세 재구성 (PR #3~#7)

| PR | 내용 |
|---|---|
| #3 | 카드에서 이름이 두 번 나오던 표기 (`이재명 이재명, …`). 이름을 지우지 않고 요약 앞머리를 뗀다. `lib/actor-name.ts` |
| #4 | 상세를 2단 → 한 줄기로. 텍스트 색 1단계 → 9단계 위계. 통으로 오는 요약 문단화(`lib/paragraphs.ts`). 출처·교차검증을 제목 바로 아래로. "사건 맥락" 목록 → 세로선 타임라인 |
| #5 | 맞불 구도 · 등장 정치인(둘 다 **스키마 변경 없이**) · 팔로우 · 댓글 · 다음 분기점(021) · Pretendard self-host |
| #6 | 유사 사례 NaN(022) + 이슈 카드 헤드라인 중복(`lib/headline.ts`) |
| #7 | 크레딧 `N/1` 통일(023) + 상세 본문 좌측 쏠림 |

**#5 에서 배운 것**: #4 에서 "데이터가 없어 불가능" 으로 뺀 6개 중 **3개는 스키마 문서만 보고 내린 오판**이었다.
실제로 쿼리를 쳐보니 맞불 구도(중심 인물이 등장하는 기록을 진영별로 가르면 됨)와
등장 정치인(`politicians` 에 385명이 다 있었다)은 그냥 됐다.
Pretendard 도 "전역 변경이라 범위 밖" 이라 미뤘는데 30분 작업이었다.
→ **"없어서 못 한다" 는 스키마를 읽어서가 아니라 쿼리를 쳐봐서 판정할 것.**

**#7 좌측 쏠림의 원인**: 컨테이너 1040px 인데 제목 682px, 본문 591px 에서 끊기고
구분선·맞불 바만 1040px 를 채웠다. 핸드오프의 `20ch`/`62ch` 캡을 1040px 컨테이너에
그대로 얹은 게 원인. 컨테이너를 880px(읽기 폭)로 좁혀 한 열로 정렬했다.

**Pretendard** — 핸드오프는 jsDelivr CDN 을 지정했지만 우리 CSP 가 `font-src 'self'` 라
그대로 했으면 폰트가 아예 안 떴다. npm 패키지의 variable 동적 서브셋(92조각)을 self-host 하고
`unicode-range` 로 실제 쓰는 조각만 받는다(페이지당 15파일 384KB). `/fonts/*` 에 immutable 캐시.

### E. 크롤러 (crawler PR #1·#2·#3 전부 머지)

- **임베딩 실패를 영벡터로 덮지 않는다.** 실패하면 `None` → NULL 저장.
  RPC 가 NULL 은 원래 제외하므로 비어 있는 편이 가짜 벡터보다 안전하다.
  실패를 모아 실행 끝에 요약하고, 저장분 전량이 임베딩 없이 들어갔으면 `exit 1`.
- **요약 구조** — 처음에 기승전결로 바꿨다가 **역피라미드로 되돌렸다**.
  기승전결은 소설·에세이 구조다. 뉴스는 가장 중요한 사실이 첫 문장에 와야 한다.
  순서: 가장 중요한 사실 → 배경·이유 → 세부 → 영향 / What·Who·Why·Impact.
  분량도 4~6문장 → **2~5문장**. 길이를 채우려는 게 요약을 애매하게 만든다.
- **예정 일정 추출** — `issue_clusters.next_branch` 에 날짜 확정된 일정만.
  `sanitize_next_branch()` 가 추측성 표현("전망"·"예상"·"조만간")·과거 날짜·400일 초과를 버린다.

### F. 마이그레이션 — 로컬 검증 후 프로덕션 적용 완료 (2026-09-18)

docker 로 Postgres 16 + pgvector 를 띄워 **버그를 먼저 재현하고** 고친 뒤 확인했다.

| 파일 | 내용 | 검증한 것 |
|---|---|---|
| `021-issue-engagement.sql` | 팔로우·댓글·`next_branch` 컬럼 | 실행·재실행 멱등, 공감수 트리거(2→1), 2자 미만 거부, 1인 1공감 PK |
| `022-embedding-nan-guard.sql` | 영벡터 → NULL 정리 + RPC NaN 가드 | 수정 전 NaN 이 1위로 재현 → 수정 후 사라짐, 멱등, 새 영벡터도 차단 |
| `023-credit-unify.sql` | 크레딧 통일 (`saju_refill_daily`) | 0→1 충전, 같은 날 멱등, 보너스 3 미누적, 소비, 잔액 0 거절, 7일차 보너스+streak 리셋 |

**적용 상태** — 019(`UPGRADE-saju-v3.sql`) · 021 · 022 · 023 모두 2026-09-18 프로덕션 SQL Editor 에
적용했다. 코드는 v3/v4 어느 쪽에서도 죽지 않는 폴백을 그대로 유지한다.

**배포 순서 방어** — 019 때 코드가 마이그레이션보다 먼저 나가 전 사용자가 402 를 받을 뻔했다.
이번엔 "테이블/함수 없음" 을 오류가 아니라 `available:false` / 이전 버전 폴백으로 다뤄
미적용 상태에서도 화면이 죽지 않는다. 021 미적용 상태로 브라우저 확인까지 했다.

---

## 지금 상태와 다음 할 일

### 완료

- [x] 신규 Supabase 프로젝트 생성 + `SETUP.sql` + `SEED.sql` 적용
- [x] 키 반영 — `minnat/.env.local`, `minnat-crawler/.env`, GitHub secrets
- [x] 열린국회정보 인증키 발급 + `ASSEMBLY_API_KEY` 배선 (로컬 + GitHub secrets)
- [x] 연결·권한·페이지 검증
- [x] 브랜치 푸시 + 머지 (2026-09-17) — 아래 "브랜치" 표
- [x] 사주 풀이 개편 · 기사 상세 재구성 (2026-09-17~18)
- [x] 임베딩 장애 원인 규명 + 코드 방어 (021~023 작성·로컬 검증)
- [x] 모든 PR 머지 (2026-09-18) — `minnat` #7·#8, `minnat-crawler` #2·#3
- [x] 마이그레이션 프로덕션 적용 (2026-09-18) — 019 · 021 · 022 · 023
- [x] Anthropic · OpenAI 크레딧 충전 (2026-09-18)

### 막혀 있던 것 — 2026-09-18 전부 해제

| 항목 | 상태 |
|---|---|
| `supabase/UPGRADE-saju-v3.sql`(019) · `021` · `022` · `023` | 프로덕션 적용 완료 |
| Anthropic 크레딧 | 충전 완료 — 크롤러 분석이 다시 돈다 |
| OpenAI 크레딧 | 충전 완료 — 임베딩이 다시 채워진다 |

### 남은 것

**1. 재생성·재실행 (크레딧이 생겼으니 이제 가능)**

- [ ] 크롤러 1회 수동 실행 → 임베딩이 실제로 채워지는지 확인 (NULL/영벡터 0 건)
- [ ] 기존 클러스터 **임베딩 재생성** — 022 는 증상만 막는다(무작위 → 빈 목록).
      유사 사례가 실제로 뜨려면 과거 67건의 임베딩을 다시 채워야 한다
- [ ] 재생성 후 **dedup 재평가** — Stage 2 가 몇 달간 꺼져 있었으므로 중복 저장이 줄어드는지 본다
- [ ] 역피라미드 요약이 실제 출력에 반영되는지 1건 육안 확인

**2. 실측 후 결정**

- [ ] 크롤러 1회 수동 실행 → 마지막 줄 `[비용]` 으로 **실제 회당 비용 실측**
      (지금까지의 비용 수치는 전부 추정이다)
- [ ] 실측값을 보고 실행 빈도 결정 (현재 `0 */3 * * *` = 8회/일)
- [ ] 요약을 역피라미드로 바꾸면서 출력 토큰이 줄었다 — 실측에 반영해 볼 것
- [x] 사주 `SAJU_MONTHLY_CALL_LIMIT` 재검토 → **기본값 800** (2026-09-18, 목표 DAU 20)
- [ ] 실사용 한 달치를 보고 DAU 가정이 맞는지 확인 — 640회(80%) 도달 시 `[saju spend]` 경고 로그
- [ ] `analyzer.py` 모델(Haiku 4.5) 유지 여부 — 사주는 이미 품질 한계로 Sonnet 5 로 갈아탔다.
      실사용량이 없어 지금은 비교 불가

**3. 기타**

- [ ] Vercel 환경변수 갱신 + 재배포 (프론트 배포용)
- [ ] 로컬 `.env` 의 빈 키 채우기 — `NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`
      (GitHub secrets 에는 있어 CI 만 동작, 로컬 디버깅 불가)

### 미검증 — 정직하게 남긴다

- 이슈 **팔로우·댓글의 정상 동작 경로** — 021 은 2026-09-18 적용됐지만 API·UI 왕복은 아직 못 봤다.
  스키마·트리거·제약은 로컬 Postgres 로 검증했다.
- 사주 **크레딧 `N/1` 화면** — 023 적용 후 실제 배지·402·자정 충전 동작 미확인.
- 사주 **로그인 상태의 실제 풀이 화면** — 로컬 로그인이 안 돼 비로그인 미리보기까지만 확인.
- 기사 상세의 **점수/감경 섹션** — scored 카테고리 이슈가 DB 에 하나도 없어 못 봤다.
- **다음 분기점 노드** — 크롤러가 `next_branch` 를 채워야 화면에 나온다.

### 선택

- [ ] Batch API 전환 (50% 추가 절감, 지연만 발생 — 제출/수거 2단계 구조 개편 필요)
- [ ] `/about` 에 팩트체크 중단 사유 명시
- [ ] 계획 문서 3개 갱신

---

## 브랜치

### 머지 완료 (2026-09-17)

| 저장소 | PR | 내용 |
|---|---|---|
| `minnat` | #2 | 사주 풀이 10섹션·3문단, json-repair |
| `minnat` | #3 | 카드 이름 중복 표기 |
| `minnat` | #4 | 기사 상세 한 줄기 재구성 |
| `minnat` | #5 | 맞불 구도·등장 정치인·팔로우·댓글·분기점·Pretendard |
| `minnat` | #6 | 유사 사례 NaN + 헤드라인 중복 |
| `minnat-crawler` | #1 | 임베딩 영벡터 금지 + 예정 일정 추출 (이전 `bab409e` 도 함께 올라감) |

### 머지 완료 (2026-09-18)

| 저장소 | PR | 내용 |
|---|---|---|
| `minnat` | #7 | 크레딧 `N/1` 통일(023) + 상세 본문 좌측 쏠림 |
| `minnat` | #8 | 2026-09-17~18 문서 정리 |
| `minnat-crawler` | #2 | 요약을 기승전결 → 역피라미드 |
| `minnat-crawler` | #3 | README 를 실제 운영 문서로 |

### 열려 있음

없다. 두 저장소 모두 열린 PR 0건, 로컬 브랜치는 `main` 뿐이다.

> 푸시로 `disabled_inactivity` cron 이 되살아났고, 2026-09-18 크레딧 충전으로 실행 조건도 갖춰졌다.
> 임베딩이 통째로 실패하면 워크플로가 `exit 1` 로 끝난다 — 조용히 성공하지 않는 것이 의도다.

---

## 계획 문서 실태

| 문서 | 상태 |
|---|---|
| `SAJU_BUILD_PLAN.md` | 체크박스가 아니라 `✅` 이모지로 표기돼 있어 미완처럼 보이지만 **M0·M1·M2 핵심 대부분 완료** |
| `plan-v1.1.md` | 프론트엔드 항목(archive 뱃지, `criminal_stage` 단계 표시, "본인 입장 확인되지 않음", 방법론 페이지, 개인 종합점수 제거)이 **이미 구현돼 있는데 미완으로 표기** |
| `plan.md` | Phase 4 항목 다수가 미완 표기 — 실제 확인 필요 |

즉 남은 일은 프론트엔드 기능이 아니라 **데이터 인프라와 운영**이었다.
